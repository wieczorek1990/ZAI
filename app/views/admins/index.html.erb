<h1>Listing admins</h1>

<table class="grid">
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @admins.each do |admin| %>
  <tr>
    <td><%= link_to 'Show', admin, class:"btn" %></td>
    <td><%= link_to 'Edit', edit_admin_path(admin), class:"btn" %></td>
    <td><%= link_to 'Destroy', admin, confirm: 'Are you sure?', method: :delete , class:"btn"%></td>
  </tr>
<% end %>
</table>

<br />
<br />

<%= link_to 'New Admin', new_admin_path, class:"btn" %> <hr />
